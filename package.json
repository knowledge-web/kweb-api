{
  "name": "kweb-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "wikidata-fetch": "cd wiki && cut data.tsv -f2 | xargs -I % node wikidata-fetcher.js %",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "axios": "^1.4.0",
    "standard": "^17.1.0"
  }
}
